import{_ as t,o as a,c as s,ag as o}from"./chunks/framework.DXGyWiRo.js";const u=JSON.parse('{"title":"Mastering Claude Code in 30 minutes","description":"Learn advanced features, shortcuts, and workflows to get the most from Claude Code.","frontmatter":{"description":"Learn advanced features, shortcuts, and workflows to get the most from Claude Code.","date":"2026-01-01","tags":["Claude Code","Productivity"]},"headers":[],"relativePath":"blog/mastering-claude-code-in-30-minutes.md","filePath":"en/blog/mastering-claude-code-in-30-minutes.md","lastUpdated":1780484238000}'),i={name:"blog/mastering-claude-code-in-30-minutes.md"};function n(r,e,d,l,h,p){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="mastering-claude-code-in-30-minutes" tabindex="-1">Mastering Claude Code in 30 minutes <a class="header-anchor" href="#mastering-claude-code-in-30-minutes" aria-label="Permalink to &quot;Mastering Claude Code in 30 minutes&quot;">​</a></h1><p><em>Distilled from <a href="https://www.youtube.com/watch?v=6eBSHbLKuN0" target="_blank" rel="noreferrer">Mastering Claude Code in 30 minutes</a>, a talk by Boris Cherny — a member of technical staff at Anthropic who created Claude Code. Learn advanced features, shortcuts, and workflows to get the most from Claude Code.</em></p><h2 id="what-claude-code-actually-is" tabindex="-1">What Claude Code actually is <a class="header-anchor" href="#what-claude-code-actually-is" aria-label="Permalink to &quot;What Claude Code actually is&quot;">​</a></h2><p>It’s not a fancy autocomplete. Earlier coding assistants completed a line — or a few lines — at a time; Claude Code is built for the other end of the spectrum. It’s fully agentic, meant for building whole features, writing entire functions and files, and fixing entire bugs in one go. Boris framed it as five things: terminal-based (not an IDE itself), works with all your tools, fits into your existing workflows, general purpose, and infinitely hackable. In practice that means it runs alongside whatever editor you live in — VS Code, Xcode, JetBrains, Vim, Emacs — in any terminal, locally or over remote SSH and tmux. You don’t swap out your workflow. You just add Claude Code to whatever you’re already doing.</p><p>Under the hood it ships with a small, sharp set of built-in tools — about a dozen: bash execution, file search, file listing, file read and write, web fetch and search, a TODO tracker, and sub-agents. That’s it. The magic is in how the model strings them together autonomously. You don’t say “use this tool, then that one” — you describe the outcome and it decides how to explore, brainstorm, and execute.</p><h2 id="setting-up-properly" tabindex="-1">Setting up properly <a class="header-anchor" href="#setting-up-properly" aria-label="Permalink to &quot;Setting up properly&quot;">​</a></h2><p>Before diving into real work, run these once:</p><ul><li><code>/terminal-setup</code>: enables Shift+Enter for newlines so you’re not fighting the prompt</li><li><code>/theme</code>: set light or dark mode</li><li><code>/install-github-app</code>: lets you <code>@claude</code> on any GitHub issue or PR</li><li><code>/allowed-tools</code>: customise which tools auto-approve so you’re not clicking through confirmations constantly</li><li><code>/config</code>: turn on notifications</li></ul><p>One underrated tip Boris mentioned: enable macOS Dictation (System Settings → Accessibility → Dictation), then double-tap the dictation key and just talk to Claude Code. He does this for a lot of his prompts — speaking to it like you would another engineer. Your prompts come out more natural, more detailed, and faster to produce than typing.</p><h2 id="start-with-codebase-q-a" tabindex="-1">Start with codebase Q&amp;A <a class="header-anchor" href="#start-with-codebase-q-a" aria-label="Permalink to &quot;Start with codebase Q&amp;A&quot;">​</a></h2><p>This is the single best way to onboard yourself or your team to Claude Code. Don’t start by asking it to write code. Start by asking it questions.</p><p>At Anthropic, new technical hires used to take 2–3 weeks to get up to speed. With Claude Code, it’s down to 2–3 days. They just point it at the codebase and ask questions.</p><p>Some example prompts from Boris’s slides:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>How is @RoutingController.py used?</span></span>
<span class="line"><span>How do I make a new @app/services/ValidationTemplateFactory?</span></span>
<span class="line"><span>Why does recoverFromException take so many arguments? Look through git history to answer</span></span>
<span class="line"><span>Why did we fix issue #18363 by adding the if/else in @src/login.ts API?</span></span>
<span class="line"><span>In which version did we release the new @api/ext/PreHooks.php API?</span></span>
<span class="line"><span>Look at PR #9383, then carefully verify which app versions were impacted</span></span>
<span class="line"><span>What did I ship last week?</span></span></code></pre></div><p>Notice what these actually ask for. Claude doesn’t just grep for a string and call it done — it goes a level deeper, finding real examples of how a class is instantiated and used, the kind of answer you’d get from documentation rather than from Cmd-F. Several of them lean on git history (“why does this function have 15 arguments, and why are they named so weirdly?”) or pull a GitHub issue or PR via web fetch and cross-check it. The striking part, as Boris pointed out: nobody prompted Claude to do any of this. There’s nothing in the system prompt about reading git history — the model just knows how to use git. As he put it, “we’re lucky to be building on such a good model.”</p><p>That <code>What did I ship last week?</code> prompt is something Boris runs every Monday in his weekly standup. Claude looks through the git log, knows your username, and gives you a clean summary you can copy-paste into a doc. Zero effort.</p><p>The Q&amp;A phase also teaches you where the boundaries are: what Claude gets immediately versus what needs more hand-holding — what can be one-shotted, two-shotted, or three-shotted, and what’s better done interactively in the REPL. That mental model is worth building before you hand it a 3,000-line feature request.</p><p>One thing worth noting: there’s no indexing, no remote database, no code upload. Everything stays local. You just start it and it works.</p><h2 id="tools-and-workflows" tabindex="-1">Tools and workflows <a class="header-anchor" href="#tools-and-workflows" aria-label="Permalink to &quot;Tools and workflows&quot;">​</a></h2><p>Once you’re comfortable with Q&amp;A, move to code editing. Claude will explore the codebase, brainstorm, and then make edits, all by chaining its tools together without you having to orchestrate any of it. You steer it with plain prompts. A few Boris showed:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>Propose a few fixes for issue #8732, then implement the one I pick</span></span>
<span class="line"><span>Identify edge cases that are not covered in @app/tests/signupTest.ts, then update the tests to cover these. think hard</span></span>
<span class="line"><span>Use 3 parallel agents to brainstorm ideas for how to clean up @services/aggregator/feed_service.cpp</span></span></code></pre></div><p>Notice the range: let Claude propose a few options and you pick one; tell it to <code>think hard</code> (one of several thinking triggers — <code>ultrathink</code> is the strongest); or fan out parallel sub-agents on a gnarly cleanup. Same small handful of tools, very different jobs. (The fourth prompt on that slide, <code>commit, push, pr</code>, gets its own treatment below.)</p><p><strong>Teach Claude your tools.</strong> The moment you plug in your team’s tools is when Claude Code really starts to shine. For bash tools, just tell it about them in the prompt. Even tell it to use <code>-h</code> to figure out usage (Boris’s <code>barley</code> CLI is a made-up example, but the pattern is real):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>Use the barley CLI to check for error logs in the last training run. Use -h to check how to use it.</span></span></code></pre></div><p>For MCP tools, register them once — <code>claude mcp add barley_server -- node myserver</code> — and then reference them in your prompts. If you’re adding the same tool repeatedly, dump it in your <code>CLAUDE.md</code> so it persists across sessions.</p><p><strong>Pick the right workflow for the task.</strong> Boris highlighted three patterns:</p><p><strong>Explore → plan → confirm → code → commit.</strong> Ask Claude to propose a few fixes, pick one, then have it implement and commit. Good for non-trivial bugs where you want to understand what’s being changed before it happens.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>Figure out the root cause for issue #983, then propose a few fixes. Let me choose an approach before you code. ultrathink</span></span></code></pre></div><p><strong>Write tests → commit → code → iterate → commit.</strong> TDD-style. Write failing tests first, commit them, then implement until they pass.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>Write tests for @utils/markdown.ts to make sure links render properly (note the tests won&#39;t pass yet, since links aren&#39;t yet implemented). Then commit. Then update the code to make the tests pass.</span></span></code></pre></div><p><strong>Write code → screenshot → iterate.</strong> This one’s powerful. Give Claude a mock image and a tool to screenshot the result (like Puppeteer or an iOS simulator), and let it iterate. Two or three loops and it usually gets very close to the mock. Claude Code has been fully multimodal from the start — even in a terminal, you can drag-and-drop the mock, give it a file path, or paste the image straight in.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>Implement [mock.png]. Then screenshot it with Puppeteer and iterate till it looks like the mock.</span></span></code></pre></div><p>The key insight is: give Claude a way to check its own work. With a feedback loop it can iterate. Without one, you get one shot.</p><p>One more incantation Boris uses constantly:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>commit, push, pr</span></span></code></pre></div><p>That’s the whole prompt. Claude looks through the git log to figure out the commit format, makes the commit, pushes to a branch, and opens a PR on GitHub. No hand-holding needed.</p><h2 id="context-is-everything" tabindex="-1">Context is everything <a class="header-anchor" href="#context-is-everything" aria-label="Permalink to &quot;Context is everything&quot;">​</a></h2><p>The more context Claude has, the smarter its decisions will be. This is where the <code>CLAUDE.md</code> file comes in.</p><p><code>CLAUDE.md</code> is a special file that gets automatically read into context at the start of every session. Think of it as the first message in every conversation. Put it in your project root, check it into source control, and share it with your team.</p><p>What goes in it: common bash commands, style guides, architectural decisions, important files, MCP tools your team uses. Keep it short. If it gets too long it just burns context without adding much value.</p><p>The file hierarchy looks like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span>/&lt;enterprise root&gt;/CLAUDE.md     → shared across all projects (managed)</span></span>
<span class="line"><span>~/.claude/CLAUDE.md              → your global config</span></span>
<span class="line"><span>project-root/</span></span>
<span class="line"><span>  CLAUDE.md                      → checked in, shared with team</span></span>
<span class="line"><span>  CLAUDE.local.md                → not checked in, just for you</span></span></code></pre></div><p>Beyond auto-loaded context, you can pull things in on demand:</p><ul><li>Slash commands live in <code>.claude/commands/</code>. Type <code>/project:foo</code> or <code>/user:foo</code> to invoke them.</li><li><code>@filename</code> mentions pull specific files into context.</li><li>Nested <code>CLAUDE.md</code> files in subdirectories get pulled in automatically when Claude works in those directories.</li></ul><p>The slash commands are more powerful than they look. At Anthropic, they have a <code>label-github-issues.md</code> command that runs automatically via GitHub Actions to label issues. Write it once, run it everywhere.</p><p>Boris’s advice: take time to actually tune your context. Run your <code>CLAUDE.md</code> through a prompt improver. Ask yourself whether it’s for you or your whole team, and whether it should load automatically or on demand. Getting this right has a dramatic effect on output quality.</p><h2 id="share-with-your-team" tabindex="-1">Share with your team <a class="header-anchor" href="#share-with-your-team" aria-label="Permalink to &quot;Share with your team&quot;">​</a></h2><p>This is the leverage play. Build once, use everywhere. Configure your setup, check it into the repo, and every engineer who clones it gets the whole thing automatically: the right tools, the right memory, the right permissions.</p><p>Almost everything about Claude Code can be shared at four levels — enterprise policy, your global config, project (checked in), or project (just you):</p><table tabindex="0"><thead><tr><th></th><th>Enterprise policy (shared)</th><th>Global (just me)</th><th>Project (shared)</th><th>Project (just me)</th></tr></thead><tbody><tr><td><strong>Memory</strong></td><td><code>/Library/Application Support/ClaudeCode/CLAUDE.md</code></td><td><code>~/.claude/CLAUDE.md</code></td><td><code>CLAUDE.md</code></td><td><code>CLAUDE.local.md</code></td></tr><tr><td><strong>Slash commands</strong></td><td>—</td><td><code>~/.claude/commands/</code></td><td><code>.claude/commands/</code></td><td>—</td></tr><tr><td><strong>Permissions</strong></td><td><code>/Library/Application Support/ClaudeCode/policies.json</code></td><td><code>~/.claude/settings.json</code></td><td><code>.claude/settings.json</code></td><td><code>.claude/settings.local.json</code></td></tr><tr><td><strong>MCP servers</strong></td><td>—</td><td><code>claude mcp</code></td><td><code>.mcp.json</code></td><td><code>claude mcp</code></td></tr></tbody></table><p>Boris called this “kind of an insane matrix” — Claude Code supports this many configurations because engineering workflows differ at every company. If you’re not sure where to start, his advice was: <strong>start with shared project context.</strong> One person does a little work, checks it in, and the whole team gets the network effect.</p><p>You can also use the enterprise level to enforce guardrails. Need a test command to always auto-approve? Add it to the enterprise policy and it’s approved for every employee. Need to block a URL from ever being fetched? Add it there and no individual can override it. Same for MCP servers: check a <code>.mcp.json</code> into the repo and anyone who runs Claude Code in it is prompted to install them. At Anthropic, the apps repo ships a Puppeteer MCP server this way, so every engineer can drive a browser and screenshot UIs without setting anything up themselves.</p><p>Run <code>/memory</code> to see exactly which memory files are active in your current session and to edit any of them directly. Type <code>#</code> followed by anything to save a note mid-session — Claude will ask <em>which</em> memory file it should go into, so you stay in control of what lands where.</p><h2 id="interlude-keybindings" tabindex="-1">Interlude: Keybindings <a class="header-anchor" href="#interlude-keybindings" aria-label="Permalink to &quot;Interlude: Keybindings&quot;">​</a></h2><p>These aren’t well-advertised, so here’s a quick reference:</p><table tabindex="0"><thead><tr><th>Key</th><th>What it does</th></tr></thead><tbody><tr><td><code>Shift+Tab</code></td><td>Toggle auto-accept edits mode (edits apply automatically; bash commands still ask)</td></tr><tr><td><code>#</code></td><td>Create a memory (you choose which file it goes into)</td></tr><tr><td><code>!</code></td><td>Drop into bash mode (command runs locally and goes into context)</td></tr><tr><td><code>@</code></td><td>Add a file or folder to context</td></tr><tr><td><code>Esc</code></td><td>Cancel what Claude is doing — safe to hit anytime, mid-edit or mid-command</td></tr><tr><td><code>Esc Esc</code></td><td>Jump back in history</td></tr><tr><td><code>Ctrl+R</code></td><td>Show verbose output (what Claude sees in its context window)</td></tr><tr><td><code>--resume</code> / <code>--continue</code></td><td>Resume a specific past session, or continue the most recent one</td></tr><tr><td><code>/vibe</code></td><td>Boris’s easter egg on the slide — go find out</td></tr></tbody></table><p>The <code>Esc</code> key deserves a callout. You can hit it at any point, mid file edit or mid bash command, and it won’t corrupt anything. Boris uses it to interrupt a 20-line diff, tell Claude to change one line, and then have it redo the edit. Very handy.</p><h2 id="claude-code-sdk" tabindex="-1">Claude Code SDK <a class="header-anchor" href="#claude-code-sdk" aria-label="Permalink to &quot;Claude Code SDK&quot;">​</a></h2><p>For automation and CI, the SDK is what you want. It’s the <em>exact</em> same engine Claude Code itself runs on, exposed as a CLI utility — and it can talk to the Anthropic API, Amazon Bedrock, or Google Vertex.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">claude</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> -p</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;"> &quot;</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;">what did I do this week?</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;">&quot;</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> \\</span></span>
<span class="line"><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;">  --allowedTools</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;"> Bash</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">(</span><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">git</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;"> log:</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;">*</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">)</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> \\</span></span>
<span class="line"><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;">  --output-format</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;"> json</span></span></code></pre></div><p>Treat it like a Unix utility. Pipe in, pipe out:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">git</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;"> status</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> |</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> \\</span></span>
<span class="line"><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">  claude</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> -p</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;"> &quot;</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;">what are my changes?</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;">&quot;</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> \\</span></span>
<span class="line"><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;">  --output-format=json</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> |</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> \\</span></span>
<span class="line"><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">  jq</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;"> &#39;</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;">.result</span><span style="--shiki-light:#B5695977;--shiki-dark:#C98A7D77;">&#39;</span></span></code></pre></div><p>Boris uses this in CI pipelines, incident response, and all sorts of automation. You can pipe in log files from GCP, Sentry output, anything, and have Claude do something useful with it. The combinations are, as he put it, endless.</p><h2 id="running-claude-code-in-parallel" tabindex="-1">Running Claude Code in parallel <a class="header-anchor" href="#running-claude-code-in-parallel" aria-label="Permalink to &quot;Running Claude Code in parallel&quot;">​</a></h2><p>Power users don’t run one Claude session. They run many — what Boris called “multi-Claude.” He cheerfully admitted he’s “sort of a Claude normie” himself, usually one session with a few terminal tabs for different repos. But the power users he sees, inside and outside Anthropic, almost always run several at once.</p><p>A few patterns Boris called out:</p><ul><li>Multiple checkouts of the same repo in separate terminal tabs, one Claude per tab</li><li>Git worktrees for isolation across parallel sessions</li><li>SSH + Tmux tunnels for remote sessions</li><li>GitHub Actions jobs running in parallel</li></ul><p>This is still a bit rough to set up, and Anthropic is actively working on making it easier — but the throughput gains are real. If you have independent tasks, there’s no reason to run them sequentially.</p><h2 id="from-the-q-a" tabindex="-1">From the Q&amp;A <a class="header-anchor" href="#from-the-q-a" aria-label="Permalink to &quot;From the Q&amp;A&quot;">​</a></h2><p>A few things from the audience questions worth knowing:</p><ul><li><strong>Adoption.</strong> Roughly 80% of technical staff at Anthropic — engineers <em>and</em> researchers — use Claude Code every day. Researchers lean on tools like the notebook tool to edit and run notebooks.</li><li><strong>Why a CLI, not an IDE?</strong> Two reasons. Anthropic engineers use a huge spread of editors (VS Code, Xcode, Vim, Emacs…) and the terminal is the one common denominator. And Boris expects models to keep improving so fast that heavy editor UI may matter less before long — so they’re deliberately not over-investing in it.</li><li><strong>The hardest part to build</strong> was making bash safe. Bash can change system state in surprising ways, but approving every command by hand kills productivity. The answer is a tiered permission system: detect read-only commands, statically analyze which commands can be safely combined, and let you allow-list and block-list at different levels.</li></ul><h2 id="tl-dr" tabindex="-1">TL;DR <a class="header-anchor" href="#tl-dr" aria-label="Permalink to &quot;TL;DR&quot;">​</a></h2><p>Boris laid out 7 tips, and they build on each other in a clear progression:</p><ol><li>Use codebase Q&amp;A to get started and build intuition</li><li>Practice prompting to learn what Claude gets immediately vs. what needs guidance</li><li>Teach Claude your tools (bash CLIs, MCP servers)</li><li>Match the workflow to the task (explore+plan, TDD, screenshot iteration)</li><li>More context = smarter output, so set up your <code>CLAUDE.md</code></li><li>Take time to tune context, it compounds</li><li>Check everything into git and share with your team</li></ol><p>If you’ve been using Claude Code as a smarter autocomplete, you’re leaving a lot on the table. The agentic loop with proper tools and context is a different experience entirely.</p>`,75)])])}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
